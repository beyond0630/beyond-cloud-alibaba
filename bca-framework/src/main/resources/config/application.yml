spring:
  cloud:
    nacos:
      server-addr: ${LINUX_HOST:192.168.178.129}:8848
      discovery:
        namespace: public
        cluster-name: ${spring.application.name}
        ip: ${LOCAL_HOST:192.168.110.179}

    alibaba:
      seata:
        tx-service-group: beyond_alibaba_tx_group

seata:
  enabled: true
  enable-auto-data-source-proxy: true              # 开启数据库自动代理
  registry:
    type: nacos
    nacos:
      server-addr: ${LINEX_HOST:192.168.178.129}:8848
      namespace: public
      cluster: seata-server
      group: SEATA_GROUP
  config:
    type: nacos
    nacos:
      server-addr: ${LINEX_HOST:192.168.178.129}:8848
      namespace: public

management:
  endpoints:
    web:
      exposure:
        include: '*'

